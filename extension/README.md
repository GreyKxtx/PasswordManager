# Password Manager Browser Extension

Браузерное расширение для Password Manager с автозаполнением паролей.

## Структура

```
extension/
├── src/
│   ├── background/      # Service worker (логика, API, состояние)
│   ├── content/         # Content scripts (автозаполнение на страницах)
│   ├── popup/           # UI при клике на иконку
│   └── shared/           # Общие типы, крипта, API
├── manifest.json
└── package.json
```

## Установка

1. Установите зависимости:
```bash
cd extension
npm install
```

2. Соберите расширение:
```bash
npm run build
```

3. Загрузите расширение в Chrome:
   - Откройте `chrome://extensions/`
   - Включите "Режим разработчика"
   - Нажмите "Загрузить распакованное расширение"
   - Выберите папку `extension/dist`

## Использование

1. **Логин**: Откройте popup и войдите с email и мастер-паролем
2. **Автозаполнение**: На страницах с формами логина появится иконка 🔐 рядом с полем пароля
3. **Синхронизация**: Нажмите "Синхронизировать" в popup для обновления записей

## Безопасность

- `vaultKey` хранится **только в памяти** background script
- Токены сохраняются в `chrome.storage.local`
- Все криптографические операции выполняются локально
- Используется тот же протокол, что и веб-клиент (Argon2id + AES-GCM)

## Разработка

```bash
# Сборка
npm run build

# Сборка с отслеживанием изменений
npm run watch
```

