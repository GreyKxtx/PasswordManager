# Быстрый старт расширения

## Установка и сборка

1. Установите зависимости:
```bash
cd extension
npm install
```

2. Соберите расширение:
```bash
npm run build
```

3. Создайте иконки (или используйте временные):
   - Создайте файлы `icon16.png`, `icon48.png`, `icon128.png` в папке `icons/`
   - Или используйте любые PNG изображения нужного размера

## Загрузка в Chrome

1. Откройте `chrome://extensions/`
2. Включите "Режим разработчика" (Developer mode)
3. Нажмите "Загрузить распакованное расширение" (Load unpacked)
4. Выберите папку `extension/dist`

## Использование

1. **Логин**: 
   - Кликните на иконку расширения
   - Введите email и мастер-пароль
   - Нажмите "Войти"

2. **Автозаполнение**:
   - Откройте любую страницу с формой логина
   - Рядом с полем пароля появится иконка 🔐
   - Кликните на иконку и выберите запись для автозаполнения

3. **Синхронизация**:
   - В popup нажмите "Синхронизировать" для обновления записей с сервера

## Настройка API

По умолчанию расширение подключается к `http://localhost:3000/api`.

Для изменения URL отредактируйте `src/shared/api.ts`:

```typescript
const API_BASE_URL = 'https://your-api-domain.com/api';
```

## Разработка

Для автоматической пересборки при изменениях:

```bash
npm run watch
```

После изменений перезагрузите расширение в `chrome://extensions/`.

