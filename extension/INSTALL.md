# Инструкция по установке расширения

## Важно: Сначала нужно собрать проект!

Chrome не увидит расширение, пока не будет создана папка `dist/` с собранными файлами.

## Шаги установки:

### 1. Установите зависимости (если еще не установлены):
```bash
cd extension
npm install
```

### 2. Соберите расширение:
```bash
npm run build
```

Это создаст папку `dist/` с:
- `manifest.json` (скопируется из корня)
- `background/background.js` (скомпилированный)
- `content/content.js` (скомпилированный)
- `popup/popup.html`, `popup.css`, `popup.js` (скопируются)
- `shared/` (скомпилированные модули)
- `icons/` (скопируются)

### 3. Загрузите расширение в Chrome:

1. Откройте `chrome://extensions/`
2. Включите **"Режим разработчика"** (Developer mode) в правом верхнем углу
3. Нажмите **"Загрузить распакованное расширение"** (Load unpacked)
4. **Выберите папку `extension/dist`** (не просто `extension`!)

### 4. Проверка:

После загрузки вы должны увидеть:
- Иконку расширения в панели инструментов
- Название "Password Manager" в списке расширений
- Без ошибок в консоли

## Если manifest.json не виден:

1. Убедитесь, что вы выбрали папку `dist/`, а не `extension/`
2. Проверьте, что сборка прошла успешно: `npm run build`
3. Убедитесь, что файл `dist/manifest.json` существует

## Для разработки:

После изменений в коде:
```bash
npm run build
```

Затем в `chrome://extensions/` нажмите кнопку обновления (🔄) у расширения.

Или используйте watch режим:
```bash
npm run watch
```

